- provide(:page_title, "Fees")
- provide(:page_header, "Fees")

.page-card
  .page-card-header
    = link_to "Add New", new_master_fee_path, class: "btn btn-primary m-0", data: { turbo_frame: "remote_modal_form" }, title: "Add new fee"

  .page-card-body
    - if @fees.present?
      = render "table"
      = render "shared/pagy"
    - else
      = render "shared/empty_record"
