.table-responsive
  %table.table
    %thead
      %tr
        %th{scope: "col"}= sort_link @search, :id, "Serail"
        %th{scope: "col"}= sort_link @search, :date, "Date"
        %th{scope: "col"}= sort_link @search, :payment_type_name, "Payment Type"
        %th{scope: "col"} Total Amount
        %th{scope: "col"}= sort_link @search, :payment_method_name, "Payment Method"
        %th{scope: "col"}= sort_link @search, :status, "Status"
        %th.text-center{scope: "col"} Actions

    %tbody
      - @payments.each do |payment|
        %tr
          %td{scope: "row"}= link_to payment.serial, coaching_student_payment_path(payment.payable_id, payment), title: "Show details", data: { turbo_frame: "remote_modal_form" }
          %td= I18n.l(payment.date, default: "")
          %td= payment.payment_type_name
          %td= amount_with_currency(payment.total_amount)
          %td= payment.payment_method_name
          %td= payment.status.display

          %td.text-center
            = link_to "Show", coaching_student_payment_path(payment.payable_id, payment), title: "Show details", class: "btn btn-primary btn-sm ms-0", data: { turbo_frame: "remote_modal_form" }
            = link_to "Edit", edit_coaching_student_payment_path(payment.payable_id, payment), title: "Edit payment", class: "btn btn-light btn-sm", data: { turbo_frame: "remote_modal_form" }
            = link_to "History", change_logs_coaching_student_payment_path(payment.payable_id, payment), title: "Show history", class: "btn btn-light btn-sm me-0", data: { turbo_frame: "remote_modal_form" }
